using namespace std;
 
int main() 
{
    int n = 0;
    cin >> n;
    for (int i = 1; i <= n; ++i) {
        char buffer[10] = {0};
        if (i % 3 == 0) {
            sprintf(buffer, "Fizz");
        }
        if (i % 5 == 0) {
            sprintf(buffer, "%sBuzz", buffer);
        }
        if (buffer[0]) {
            cout << buffer << endl;
        }
        else {
            cout << i << endl;
        }
    }
    return 0;
}
